{% extends 'base.html.twig' %}

{% block title %}Commander Pilulier{% endblock %}

{% block body %}
    <h1>Pilulier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pilulier.id }}</td>
            </tr>
            <tr>
                <th>ActivationBoutonUrgence</th>
                <td>{{ pilulier.activationBoutonUrgence ? 'Yes' : 'No' }}</td>
            </tr>

            <tr>
                <th>
                    <button class="btn"><a href="{{ path('app_pilulier_open', {'id': pilulier.id}) }}">Ouvrir le Pillulier</a></button>
                    <button class="btn"><a href="{{ path('app_pilulier_close', {'id': pilulier.id}) }}">Fermer le Pillulier</a></button>
                </th>
            </tr>
            <tr>
                <td><button class="btn"><a href="{{ path('app_pilulier_change_hours', {'id': pilulier.id}) }}">Changer les heures de délivrement</a></button></td>
            </tr>
            <tr>
                <td>
                    <button class="btn"><a href="{{ path('app_pilulier_power', {'id': pilulier.id}) }}">Power</a></button>
                    <button class="btn"><a href="{{ path('app_pilulier_remote', {'id': pilulier.id}) }}">Remote</a></button>
                    <button class="btn"><a href="{{ path('app_pilulier_test_motor', {'id': pilulier.id}) }}">Test Moteur</a></button>
                </td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_pilulier_index') }}">back to list</a>

    <a href="{{ path('app_pilulier_edit', {'id': pilulier.id}) }}">edit</a>

    {{ include('pilulier/_delete_form.html.twig') }}
{% endblock %}