{% extends 'base.html.twig' %}

{% block title %}Traitement{% endblock %}

{% block body %}
    <h1>Traitement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traitement.id }}</td>
            </tr>
            <tr>
                <th>IdTraitement</th>
                <td>{{ traitement.idTraitement }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ traitement.description }}</td>
            </tr>
            <tr>
                <th>Posologie</th>
                <td>{{ traitement.posologie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_traitement_index') }}">back to list</a>

    <a href="{{ path('app_traitement_edit', {'id': traitement.id}) }}">edit</a>

    {{ include('traitement/_delete_form.html.twig') }}
{% endblock %}
